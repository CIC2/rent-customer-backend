apiVersion: apps/v1
kind: Deployment
metadata:
  name: rent-customer-app
  namespace: dev-rent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rent-customer-app
  template:
    metadata:
      labels:
        app: rent-customer-app
    spec:
      # <- add the imagePullSecrets here
      imagePullSecrets:
        - name: icr-secret
      containers:
        - name: rent-customer-app
          image: de.icr.io/dev-registry/rent-customer-backend:latest
          args:
            - --spring.application.json=$(BINDING)
          env:
            - name: BINDING
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: application-properties
            - name: DB_CONNECTION
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: DB_CONNECTION
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: DB_PASSWORD
          ports:
            - containerPort: 8025
          resources:
            requests:
              cpu: 125m
              memory: 350Mi
---
apiVersion: v1
kind: Service
metadata:
  name: rent-customer-service-cip
  namespace: dev-rent
  labels:
    app: rent-customer-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8025
  selector:
    app: rent-customer-app


